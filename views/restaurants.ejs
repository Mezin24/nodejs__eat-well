<!DOCTYPE html>
<html lang="en">
  <head>
    <%- include('includes/head') %>
    <link rel="stylesheet" href="/styles/restaurants.css" />
  </head>
  <body>
    <%- include('includes/header.ejs') %> <%- include('includes/side-drawer') %>
    <main>
      <h1>Recommended restaurants</h1>
      <% if(restaurants.length === 0) { %>
      <p>
        Unfortunately, we have no restaurants yet - maybe start recomending
        some?
      </p>
      <% } else { %>
      <p>Find your next favorite restaurants with help of our other users!</p>
      <p>We found <%= numberOfRestaurants %> for you.</p>
      <ul id="restaurants-list">
        <% restaurants.forEach(restaurant => { %> <%-
        include('includes/restaurants/restaurant-item', {restaurant}) %> <% })
        %>
      </ul>
      <% } %>
    </main>
  </body>
</html>
